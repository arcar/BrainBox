const dbMiddleware = (req, res, next) => {
    
    if (!client) {
        return res.status(503).json({ error: "Base de données non disponible." });
    }

    req.db = client.db('brainbox'); 
    next();
};

module.exports = dbMiddleware;